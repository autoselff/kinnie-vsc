## kinnie-vsc extension for Visual Studio Code
The extension for Visual Studio Code is available for download from the **Releases** tab. To install the extension, simply type the command `code --install-extension kinnie-vsc-1.0.0.vsix`.
